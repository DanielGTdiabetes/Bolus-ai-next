<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <permission android:name="org.bolusai.next.senderfixture.REQUEST_IDENTITY" android:protectionLevel="signature" />
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@android:drawable/ic_menu_info_details"
        android:label="Bolus Next sender test"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <receiver
            android:name=".SyntheticSenderReceiver"
            android:exported="true"
            android:permission="org.bolusai.next.senderfixture.REQUEST_IDENTITY" />
    </application>
</manifest>
